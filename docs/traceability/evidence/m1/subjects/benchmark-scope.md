# M1 Native document-service sample scope

The 21 positive nanosecond samples were emitted on 2026-07-15 by the exact locked release command recorded in both benchmark reports. Commit `926472cf541ddd1b3cb2da3f989bdcec74746701` anchors the timed Native implementation, unchanged independent reference, existing fixture bytes and expected outputs, and v0.62 pre-promotion trace state; it is not claimed to contain the complete executable evidence setup. The maturity and support harnesses, four registered case provenance edits, new holdout and adjudication cases, fuzz assets, v0.63 trace maps, and evidence were still uncommitted, and every executable input is bound by its canonical content hash. One untimed reference-checked warmup ran first. Each timed sample then created a fresh `M1StrictDocumentSession`, opened the same content-addressed independent nested-four-pages holdout, obtained terminal Native page-count and outline results, verified both against the independent reference, and retained the duration only after equality passed. Sessions were not reused.

The reports preserve the raw execution order and recompute nearest-rank p95/p99. Their median is the eleventh ordered value. The conservative two-sided distribution-free median interval uses the sixth and sixteenth ordered values; for 21 observations its exact binomial coverage is 97.34%, so it satisfies the recorded 95% confidence interval without a normality assumption.

The host/build metadata describes macOS 26.4.1 on an Apple M2 Max with Rust 1.93.0 and the release profile. `memory_bytes` records host physical memory; this batch does not claim isolated peak RSS. No renderer, font, color, GPU, or browser path was exercised.

The distribution is eligible only as raw same-Native-scope performance evidence. No external engine was timed, no fixed worker-pool throughput claim was made, no regression threshold or release gate is derived, and no Native/external ratio exists. These samples do not register PDFium, an O4 probe, or an external performance baseline.
