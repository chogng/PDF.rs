[package]
name = "pdf-rs-quality"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
pdf-rs-compare = { path = "../compare" }
pdf-rs-digest = { path = "../digest" }
pdf-rs-generate = { path = "../generate" }

[dev-dependencies]
pdf-rs-benchmark = { path = "../benchmark" }
pdf-rs-corpus = { path = "../corpus" }
