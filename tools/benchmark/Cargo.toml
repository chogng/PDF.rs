[package]
name = "pdf-rs-benchmark"
version = "0.1.0"
edition = "2024"
rust-version = "1.93"
publish = false

[dependencies]
pdf-rs-corpus = { path = "../corpus" }
pdf-rs-digest = { path = "../digest" }
